trigger:
- master

pool:
  vmImage: 'ubuntu-latest'
stages:
- stage: Build
  jobs:
    - job: firstjob
      steps:
      - bash: echo Build First Job
    - job: SecondJob
      steps:
      - bash: echo Build Second Job
- stage: DevDeploy
  variables:
    variable1: Dev
  dependsOn: Build
  jobs:
    - job: DevFist
      steps:
      - bash: echo $(variable1)DeployJob
    - job: DevSeccond
      steps:
      - bash: echo Dev Second Job
- stage: QADeploy
  variables:
     variable1: 
  dependsOn: Build
  jobs:
    - job: QAJob
      steps:
      - bash: echo $(variable1)QADeploy
- stage: ProdDeploy
  dependsOn:
  - DevDeploy
  - QADeploy
  jobs:
    - job: ProductionsJob
      steps:
      - bash: echo $(variable1)ProdDeploy